const assert = require('assert');
const npmMove = require('../index.js');

describe('npmMove', function () {
  it('has a test', function () {
    assert(false, 'npmMove should have a test');
  });
});
